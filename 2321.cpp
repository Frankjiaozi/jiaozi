#include <stdio.h>
#include <string.h>
int main()
{
    char str[1000];
    while(scanf("%s",str)!=EOF)
    {   int flag=0;
        for(int i=0;i<strlen(str);i++)
        {
            if(str[i]=='w'&&str[i+1]=='e') 
        }
    }
}